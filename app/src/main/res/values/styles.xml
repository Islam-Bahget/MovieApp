<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
       <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@android:color/white</item>
    </style>

    <style name="Detail" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@android:color/white</item>



        <item name="homeAsUpIndicator">@drawable/back</item>
    </style>

    <style name="ToolBar" parent="Widget.AppCompat.SearchView">
        <item name="android:textColorPrimary">#FFF</item>
        <item name="android:actionMenuTextColor">#FFF</item>
        <item name="android:textColorSecondary">#FFF</item>
    </style>

    <style name="ToolBarTrend" parent="Widget.AppCompat.SearchView">
        <item name="android:textColorPrimary">#000</item>
        <item name="android:actionMenuTextColor">#000</item>
        <item name="android:textColorSecondary">#FFF</item>
    </style>

</resources>
